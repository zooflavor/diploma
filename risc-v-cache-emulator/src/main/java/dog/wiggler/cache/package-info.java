/**
 * These classes implement the various caches.
 * These caches can process a memory access log,
 * running the log entries through a cache,
 * and writing a new log containing the memory accesses of the cache.
 */
package dog.wiggler.cache;